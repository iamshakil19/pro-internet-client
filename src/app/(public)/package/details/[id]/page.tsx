import PackageDetailsComponent from "@/components/publiLayoutComponent/Package/PackageDetailsComponent";

const PackageDetails = ({ params }: any) => {
  return <PackageDetailsComponent params={params} />;
};

export default PackageDetails;
